// Stub file, because CMake+IDE Intellisense does not work correctly with header-only libraries,
// unless they are included in a C++ file, even when the library is defined as "INTERFACE"
#include "vec3.hpp"
